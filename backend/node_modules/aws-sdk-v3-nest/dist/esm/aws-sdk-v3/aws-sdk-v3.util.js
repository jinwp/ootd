import { isClassDefinition, } from "./aws-sdk-v3.type.js";
var AWS_SDK_V3_MODULE = "AWS_SDK_V3_MODULE";
export function getClientToken(client, key) {
    if (key === void 0) { key = ""; }
    if (isClassDefinition(client)) {
        return "".concat(AWS_SDK_V3_MODULE, "#").concat(client.name, "#").concat(key);
    }
    // @ts-ignore
    var className = client.__proto__.constructor.name;
    return "".concat(AWS_SDK_V3_MODULE, "#").concat(className, "#").concat(key);
}
